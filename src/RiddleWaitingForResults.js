import React from 'react'

function RiddleWaitingForResults (props) {

    return(<div className="col-xs-12 r18 riddle-wait-for-results">
        Odpowiedź oraz wyniki będą dostępne kilka minut po zakończeniu zagadki.
    </div>)
}

export default RiddleWaitingForResults;